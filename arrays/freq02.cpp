#hashmap -> it is a data structure that stores key-value pairs. It allows for efficient retrieval of values based on their corresponding keys. In C++, the unordered_map is a commonly used implementation of a hashmap.

